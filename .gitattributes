# Normalize line endings so a Windows checkout produces the same ZIP bytes as CI.
# Without this, core.autocrlf rewrites endings and the local digest diverges from
# the signed release digest, failing signature verification at host install.
* text=auto eol=lf
*.png binary
*.jpg binary
*.zip binary
*.sig binary
